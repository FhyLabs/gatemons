<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>GateMons – Gateway Monitoring System</title>
	<meta name="description" content="GateMons is a real-time multi-tenant telemetry gateway for devices and servers. Secure, fast, and zero permanent storage architecture with isolated state per tenant.">
	<meta name="keywords" content="telemetry gateway, real-time telemetry, multi-tenant gateway, IoT gateway, device telemetry, zero storage architecture, state streaming">
	<meta name="author" content="Fhylabs">
	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;600;700&family=JetBrains+Mono:wght@400;600&display=swap" rel="stylesheet">
	<style>
		body { font-family: 'Space Grotesk', sans-serif; }
		.mono { font-family: 'JetBrains Mono', monospace; }
		.text-stroke-cyan {
			-webkit-text-stroke: 2px rgba(0, 255, 255, 0.55);
		}
		.neon-cyan {
			text-shadow:
			0 0 3px rgba(0, 255, 255, 0.55),
			0 0 10px rgba(0, 255, 255, 0.45),
			0 0 10px rgba(0, 255, 255, 0.35),
			0 0 10px rgba(0, 255, 255, 0.25);
		}
	</style>

	<link rel="icon" type="image/png" href="/assets/images/favicon.png" sizes="32x32">
	<link href="/assets/css/styles.css" rel="stylesheet">
</head>

<body class="min-h-screen flex flex-col w-full bg-black text-white overflow-auto select-none relative items-center justify-center">
	<video src="/assets/videos/hero.mp4" autoplay loop muted playsinline class="fixed top-0 left-0 w-full h-full object-cover opacity-50 -z-20"></video>
	<div class="absolute inset-0 bg-[radial-gradient(circle_at_50%_30%,rgba(0,255,255,0.18),transparent_65%)] -z-10"></div>
	<div class="absolute inset-0 bg-gradient-to-b from-black/70 via-black/40 to-black/90 -z-10"></div>

	<main class="relative flex flex-col items-center justify-center text-center px-4 md:px-12 py-12 md:py-20 container mx-auto">
		<span class="text-xs md:text-base bg-white/5 backdrop-blur-[18px] px-4 py-1 rounded-full border border-cyan-300/20
			shadow-[0_0_30px_rgba(0,255,255,0.18)]">
			Safe, Fast, and Secure Real-Time Telemetry
		</span>
		<div class="relative mt-8 flex flex-col justify-center items-center">
			<img class="h-36 md:h-52 w-36 md:w-52 neon-glow-cyan" src="/assets/images/favicon.png" alt="gatemons" />
			<h1
			  class="absolute z-50 -bottom-6 md:-bottom-10 text-4xl md:text-6xl md:text-7xl font-extrabold tracking-widest
			  bg-clip-text text-transparent bg-gradient-to-r from-cyan-400 via-sky-400 to-cyan-400
			  text-stroke-cyan neon-cyan">
			  GATEMONS
			</h1>
		</div>
		<p class="text-base md:text-xl max-w-3xl mt-9 md:mt-12 leading-relaxed text-gray-200 drop-shadow">
			A real-time multi-tenant gateway for telemetry ingestion. Securely receiving data from any device
			and serving the latest state to servers with zero permanent storage.
		</p>
		
		<div class="mt-6 px-10 py-7">
			<p class="text-xs md:text-xl tracking-widest font-semibold text-cyan-300 drop-shadow">
				CLIENT → GATEMONS ← SERVER
			</p>
			<p class="mb-6 text-xs md:text-sm text-gray-300 mt-1">
				Isolated tenant states, no cross-contamination.
			</p>
			<a href="/docs"
				class="relative inline-block px-10 py-4 font-extrabold tracking-widest uppercase mono
				text-black bg-gradient-to-r from-sky-400 via-cyan-300 to-sky-400 rounded-xl
				shadow-[0_0_20px_rgba(0,255,255,0.45)]
				transition-all duration-300 ease-out
				hover:scale-[1.05]
				hover:shadow-[0_0_45px_rgba(0,255,255,0.9)]
				active:scale-[0.98]
				after:absolute after:inset-0 after:rounded-xl after:animate-pulse
				after:bg-gradient-to-r after:from-sky-400 after:via-cyan-300 after:to-sky-400
				after:opacity-60 after:-z-10">
				GET STARTER
			</a>
			<p class="mono mt-6 text-xs md:text-base text-cyan-300 drop-shadow tracking-wider">
				<span class="text-white">Status</span> <span id="statusVal"><%= status %></span>
				<span class="text-white">Uptime</span> <span id="uptimeVal"><%= uptime %></span>
			</p>
		</div>
		<div class="grid grid-cols-1 md:grid-cols-3 gap-4 mt-8 max-w-5xl text-gray-200">
			<div class="p-5 bg-white/5 backdrop-blur-lg rounded-xl border border-cyan-400/30
				hover:shadow-[0_0_25px_rgba(0,255,255,0.25)] hover:border-cyan-300 transition-all">
				<h4 class="text-xs md:text-xl text-cyan-300 font-semibold mb-1">Security</h4>
				<p class="text-xs md:text-sm">HMAC signature · Strict header authentication · Per-tenant isolation.</p>
			</div>
			<div class="p-5 bg-white/5 backdrop-blur-lg rounded-xl border border-cyan-400/30
				hover:shadow-[0_0_30px_rgba(0,255,255,0.35)] hover:border-cyan-300 transition-all">
				<h4 class="text-xs md:text-xl text-cyan-300 font-semibold mb-1">Performance</h4>
				<p class="text-xs md:text-sm">Async pipeline · in-memory buffer · zero permanent storage.</p>
			</div>
			<div class="p-5 bg-white/5 backdrop-blur-lg rounded-xl border border-cyan-400/30
				hover:shadow-[0_0_25px_rgba(0,120,255,0.25)] hover:border-blue-300 transition-all">
				<h4 class="text-xs md:text-xl text-cyan-300 font-semibold mb-1">Reliability</h4>
				<p class="text-xs md:text-sm">No data mixing · Auto cleanup · Audit logs & metrics per tenant.</p>
			</div>
			
		</div>
		<footer class="mt-12 text-[8px] md:text-[11px] tracking-widest text-gray-400 uppercase mono">
			Send from anywhere — fetch from anywhere — as long as you speak the protocol.
		</footer>
	</main>
	<script>
	(() => {
		const uptimeEl = document.getElementById("uptimeVal");
		if (!uptimeEl) return;

		const [daysPart, timePart] = uptimeEl.textContent.split(" ");
		const days = Number(daysPart.replace("days", ""));
		const [h, m, s] = timePart.split(":").map(Number);

		let seconds = (days * 86400) + (h * 3600) + (m * 60) + s;

		function format(sec) {
			const d = Math.floor(sec / 86400);
			const hrs = Math.floor((sec % 86400) / 3600).toString().padStart(2, "0");
			const mins = Math.floor((sec % 3600) / 60).toString().padStart(2, "0");
			const secs = Math.floor(sec % 60).toString().padStart(2, "0");
			return `${d}days ${hrs}:${mins}:${secs}`;
		}

		setInterval(() => {
			seconds++;
			uptimeEl.textContent = format(seconds);
		}, 1000);
	})();
	</script>
</body>
</html>
